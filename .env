# Environment Configuration

NODE_ENV=development
DATABASE_URL=postgresql://clinic_user:clinic_password_2024@localhost:5432/clinic_management
PORT=3000
HOST=0.0.0.0

# Clinic Timezone Configuration
# Use IANA timezone identifiers (e.g., Africa/Juba, Africa/Nairobi, etc.)
# Default: Africa/Juba (UTC+2) - recommended for South Sudan clinics
# Note: South Sudan switched from UTC+3 to UTC+2 on February 1, 2021
# 
# This timezone is used to determine "Today", "Yesterday", and other date presets.
# All timestamps are stored in UTC in the database and converted to/from this 
# timezone only when computing date ranges or displaying dates.
#
# Examples:
#   Africa/Juba     - South Sudan (UTC+2)
#   Africa/Nairobi  - Kenya (UTC+3)
#   Africa/Kampala  - Uganda (UTC+3)
#   Africa/Cairo    - Egypt (UTC+2)
#
CLINIC_TZ=Africa/Juba

# Client-side timezone (must match CLINIC_TZ)
# Vite exposes environment variables prefixed with VITE_ to the client
VITE_CLINIC_TZ=Africa/Juba

# Debug Time Banner (optional, for QA validation)
# When set to 'true', shows a yellow banner at top of app displaying:
# - Browser local time
# - UTC time
# - Africa/Juba time
# - Current Clinic Day Key
# This helps validate that day boundaries are computed correctly.
# Default: false (off in production)
VITE_SHOW_TIME_DEBUG=false
